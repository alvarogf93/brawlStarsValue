# MÁSTER TO-DO LIST — Auditoría Panóptico

> Auditor: Panóptico (consolidación por orquestador)
> Fecha: 2026-04-28T11:47:00Z
> Repo: C:\Proyectos_Agentes\brawlValue
> Commit: 604fe24abe1a37a5963b07f199201eaca5323f13
> Rama: main
> Reportes fuente: `fase-1-arquitectura.md`, `fase-2-logica.md`, `fase-3-seguridad.md`, `fase-5-testing.md`

---

## Resumen

| Criticidad | Hallazgos |
|------------|-----------|
| 🔴 CRÍTICA | 4 |
| 🟠 ALTA    | 24 |
| 🟡 MEDIA   | 26 |
| 🟢 BAJA    | 15 |
| **Total**  | **69** |

| Categoría | Hallazgos |
|-----------|-----------|
| Arquitectura | 12 |
| Datos / Lógica | 16 |
| Seguridad | 11 |
| Rendimiento | 10 |
| Resiliencia | 9 |
| Testing | 15 |

> Tres entradas se fusionaron por dedupe: **MIX-01** (ARQ-11 ⊕ PERF-02 — rate-limit in-memory), **MIX-02** (ARQ-06 ⊕ LOG-08 — manual sync no escribe meta_stats), **MIX-03** (LOG-03 ⊕ PERF-08 — brawler-detail trunca pickRate). **TEST-01** se recalibró de CRÍTICA → ALTA: la ausencia de CI no produce downtime/RCE/corruption por sí sola; degrada el sistema bajo cambios futuros.

---

## Tabla consolidada

> Orden: Criticidad descendente (CRÍTICA → ALTA → MEDIA → BAJA), luego categoría alfabética.
> Cada entrada conserva su detalle completo (evidencia, snippet, justificación) en el reporte de fase original.

| ID | Criticidad | Categoría | Ubicación | Problema Detectado | Acción Requerida |
|----|------------|-----------|-----------|-------------------|------------------|
| LOG-01 | 🔴 CRÍTICA | Datos | `supabase/migrations/021_brawler_trends_hardening.sql:42-51` y `migrations/020_*.sql:76-85` | `compute_brawler_trends()` no filtra `source`, mezcla datos PRO (`global`) con datos personales de usuarios premium (`users`); el badge "trend PRO" del home está contaminado | Migración `CREATE OR REPLACE FUNCTION` añadiendo `AND source = 'global'`. Mismo fix en TS (LOG-04). Test SQL/integración con dos rows mismo brawler distinto source. **S** |
| LOG-02 | 🔴 CRÍTICA | Resiliencia | `src/app/api/webhooks/paypal/route.ts:43-92` | Marca `webhook_events` ANTES del `profiles.update`; si éste falla, el reintento de PayPal ve `23505` y devuelve `skipped:true` — el usuario paga pero queda `tier:'free'` permanentemente | Insertar marca de idempotencia DESPUÉS del update exitoso (o envolver en transacción/RPC). Test de integración con fallo simulado en `profiles.update`. **S** |
| MIX-03 | 🔴 CRÍTICA | Datos + Rendimiento | `src/app/api/meta/brawler-detail/route.ts:36-67` | Query `totalBattlesData` 14 días sin paginar tropieza con cap de 1000 filas de PostgREST → `allGames` corrompido → `pickRate` mostrado al usuario es matemáticamente falso. Las 3 queries son además seriadas (paralelizables) | RPC SQL `sum_meta_stats_total(p_since DATE)` (patrón ya usado en migraciones 014/017). `Promise.all([rawStats, rawMatchups, totalBattlesData])`. **S** |
| SEG-01 | 🔴 CRÍTICA | Seguridad | `src/app/api/auth/callback/route.ts:6-11, 51` | `${origin}${next}` con `next="//evil.com/phish"` produce `https://brawlvision.com//evil.com/phish` que browsers interpretan como redirect a `evil.com`. Vector OAuth-phishing CWE-601 explotable hoy con un único URL | Validar con `new URL(next, origin)` y comprobar `parsed.origin === origin`; rechazar paths `//`, `/\\`. Aplicar al branch `if (!code)` también. **S** (≤ 15 LoC) |
| ARQ-01 | 🟠 ALTA | Arquitectura | `src/lib/supabase/types.ts:131-155` | `Database` declara 4 tablas; el código consulta 11. `meta_stats`, `meta_matchups`, `meta_trios`, `cron_heartbeats`, `brawler_trends`, `meta_poll_cursors`, `anonymous_visits` van sin tipar — `strict:true` queda invalidado en las rutas más críticas | `npx supabase gen types typescript --project-id <id>` → `supabase/types.gen.ts`. Parametrizar `createClient<Database>()`. Propagar fixes (~6 rutas). **M** (1 día) |
| ARQ-02 | 🟠 ALTA | Arquitectura | `src/app/api/meta/pro-analysis/route.ts:31-582` | God Object 582 LoC: validación + auth + 4 queries + Tier 1/2 + agregación + premium gating + 4 helpers de gap + serialización en un único `GET()` | Extraer `src/lib/meta/pro-analysis/{aggregate,counters,personalGap,trios}.ts`. Route queda como orquestador 80-100 LoC. **M** (4-6 h) |
| ARQ-03 | 🟠 ALTA | Código Limpio | `src/app/api/meta/route.ts:16-154` ↔ `src/app/[locale]/picks/page.tsx:53-162` | Duplicación deliberada `SPARSE_THRESHOLD=30` + Tier 1/2 + agregador (>100 LoC). CLAUDE.md la "permite" pero ya creció — divergencia por disciplina manual frágil | Extraer `src/lib/meta/cascade.ts::buildEventsWithCascade(supabase, draftEvents)`. Ambas paths lo consumen. **S** (2-3 h) |
| ARQ-05 | 🟠 ALTA | Arquitectura | 8 ficheros: `[locale]/{leaderboard,profile/[tag]/{battles,brawlers,club,cosmetics,page,share,stats}}/page.tsx` | 8 páginas usan `AdPlaceholder` directo violando la decisión-record de CLAUDE.md (`SafeAdSlot` mandatorio). `cosmetics`/`share` lo renderizan sin gate `hasContent`; riesgo real de re-incurrir el ban AdSense que ya pasó | Sustituir `<AdPlaceholder>` por `<SafeAdSlot hasContent={…}/>` con condición correcta. Renombrar a `_AdPlaceholder` para forzar el wrapper. **M** (4 h) |
| ARQ-08 | 🟠 ALTA | Datos + Arquitectura | `src/lib/brawler-detail/trend.ts:32-62` ⊕ `supabase/migrations/021_*.sql` | `compute7dTrend` vive en TS y en pl/pgSQL sin enforcement; cualquier cambio en `MIN_BATTLES_PER_TREND_WINDOW` o rounding solo en uno produce trends DIFERENTES en `/api/meta/brawler-trends` (PRECOMPUTED) vs `/api/meta/brawler-detail` para el mismo brawler | Test integración Postgres-backed: llamar `compute_brawler_trends()` SQL via service-role y comparar contra `compute7dTrend()` TS sobre fixtures idénticas. Migration de seed + test vitest que llama el RPC. **M** (4-6 h) |
| ARQ-14 | 🟠 ALTA | Rendimiento + Arquitectura | `src/hooks/useClubEnriched.ts:54-73` | `useClubEnriched` invoca `/api/calculate` por miembro (3 Supercell calls + cómputo gemas) — explícitamente prohibido por CLAUDE.md fuera de landing. Club de 30 miembros = 18-54 s | Crear `/api/player/club-summary` que solo expone los 9 campos sin gem-value, o ampliar `/api/player/tag-summary`. **M** (3-4 h) |
| LOG-04 | 🟠 ALTA | Datos | `src/app/api/meta/brawler-trends/route.ts:91-105` | Rama "fallback inline" del endpoint también omite `.eq('source','global')`. Si LOG-01 se corrige sin tocar esto, fast-path y fallback divergirán | Añadir `.eq('source','global')` antes del `.gte('date', cutoff)`. Misma PR que LOG-01. **S** |
| LOG-05 | 🟠 ALTA | Resiliencia | `src/app/api/auth/callback/route.ts:37-49` | Retry de `exchangeCodeForSession` sobre código PKCE/OAuth single-use ya consumido — siempre devolverá `invalid grant`. Oculta la causa raíz del primer fallo y siempre redirige a `?auth_error=1` | Eliminar el retry. Distinguir tipo de `error` (network vs auth). Manejar resilience en cliente Supabase JS antes de la llamada server. **S** |
| LOG-06 | 🟠 ALTA | Resiliencia | `src/app/api/cron/meta-poll/route.ts:97-200` | `processOnePlayer` envuelve toda la lógica en `try/catch` con bloque vacío sin logging. 600+ jugadores/run; un 403 sistemático de Supercell para una cohorte de país falla en silencio agregado a `errors:N` sin tipo de error | `catch (err) { console.warn('[meta-poll] processOnePlayer failed', { tag, err: String(err) })}`. Si `SuprecellApiError`, capturar `.status` y agregar al diagnostic block. **S** |
| LOG-07 | 🟠 ALTA | Rendimiento | `src/lib/analytics/compute.ts:558-602, 811-826, 891-924` | `computeTiltAnalysis` recomputa `computeSessions(battles)` (ya hecho en l. 32) y hace `battles.filter(...)` dentro del loop sobre sesiones — O(N×M). Usuario premium con 50k battles × 1500 sesiones ≈ 75M ops × 3 helpers | Pasar `sessions` precomputado como argumento. Indexar battles a sesiones en una pasada con puntero. Una pasada para los 3 helpers combinados. **M** (4-6 h) |
| LOG-09 | 🟠 ALTA | Resiliencia | `src/app/api/checkout/paypal/confirm/route.ts:46-57` | El `update` de `profiles` no destructura `error` — si falla silenciosamente el código sigue al `?upgraded=true`. Usuario ve éxito pero sigue free | Destructure `{ error }` y redirect `?payment_error=1` si falla. Logear con subscriptionId/profileId para reconciliación. **S** |
| MIX-01 | 🟠 ALTA | Arquitectura + Seguridad + Rendimiento | `src/app/api/analytics/route.ts:7-9, 24-30, 43` | Rate-limiter `Map<string,number>` in-memory: Vercel arranca múltiples instancias por región, cada una tiene su mapa, atacante autenticado dispara N requests en paralelo y todas pasan. La protección es teatro; comentario "Per-user rate limit" engaña al lector | Migrar a Upstash Redis (creds ya en `.env.example`). Patrón `@upstash/ratelimit` aplicable también a `/api/calculate`, `/api/battlelog`, `/api/club`, `/api/sync`, `/api/profile/check-premium`. **M** (módulo + 5 callsites) |
| MIX-02 | 🟠 ALTA | Datos + Arquitectura | `src/lib/battle-sync.ts:1-62` ⊕ `src/app/api/cron/sync/route.ts:79-132` | Manual sync (`/api/sync`) avanza `last_sync` pero NO escribe `meta_stats`; el siguiente cron lee `last_sync` ya avanzado y descarta TODAS las battles que se acaban de sincronizar (`b.battle_time <= last_sync`). Pérdida silenciosa de aporte al meta global | Helper compartido `syncBattlesAndMeta(supabase, playerTag, lastSync)` que ambas rutas consumen. Devolver `{ fetched, inserted, metaRowsWritten }`. **M** (3-4 h) |
| PERF-01 | 🟠 ALTA | Resiliencia | `src/lib/api.ts:45-57`, `src/lib/paypal.ts:22-27,44-56,72-104,117-134,159-160,182-197`, `src/lib/telegram.ts:12-20`, `src/lib/telegram/sender.ts:22-31`, `src/lib/draft/brawler-names.ts:13`, `src/app/api/maps/route.ts:13`, `src/app/api/draft/maps/route.ts:16` | CERO timeouts en TODAS las llamadas externas (Supercell, PayPal, Telegram, BrawlAPI). Una cuelga 60s consume 20% del budget cron 270s. PayPal sandbox flaquea → webhook bloqueado → PayPal reintenta agresivamente | `src/lib/http.ts` con `fetchWithTimeout(url, opts, ms=8000)` usando `AbortSignal.timeout(ms)`. Retry exponencial con jitter solo idempotentes (GET, OAuth client_credentials, verify-webhook). Circuit-breaker para Supercell. **L** |
| RES-01 | 🟠 ALTA | Resiliencia + Seguridad | `src/lib/paypal.ts:175-201` ⊕ `src/app/api/webhooks/paypal/route.ts:13-33` | `verifyPayPalWebhook` parsea/re-stringify el body ANTES de mandarlo a PayPal verify-API. La verificación criptográfica del rawBody original NO se hace localmente; descansa 100% en confiar en la verificación remota | Validar firma localmente contra `rawBody` (PKCS#1 v1.5 + cachear `paypal-cert-url`). SDK oficial: `paypal-rest-sdk.notification.webhookEvent.verify(headers, rawBody, webhookId)`. Eliminar JSON.parse/stringify intermedio. **M** |
| SEG-02 | 🟠 ALTA | Seguridad | `package.json:36` (`"next": "16.2.2"`) | CVE GHSA-q4gf-8mx6-v5v3 (CVSS 7.5 high — DoS Server Components). Rango vulnerable `>=16.0.0-beta.0 <16.2.3`. Fix `16.2.4` no semver-major | `npm install next@16.2.4 next-intl@^4.9.1`. Correr `npx vitest run` + `npx tsc --noEmit` + `npx playwright test` para regresiones. **S** |
| SEG-03 | 🟠 ALTA | Seguridad | `src/app/api/auth/callback/route.ts:10-12, 46-47` | `next.split('/')[1] \|\| 'es'` confía en que el atacante use uno de 13 locales válidos. `next="/admin/secret"` → `${origin}/admin?auth_error=1`. Branch `if (!code)` redirige sin OAuth y sin validar — agrava SEG-01 | Validar `locale` contra `SUPPORTED_LOCALES` ya constante. Eliminar branch redirect en `if (!code)` (devolver 400 explícito). **S** |
| SEG-04 | 🟠 ALTA | Seguridad | `src/app/api/checkout/paypal/confirm/route.ts:13-57` | Usa `profile_id` del query string + service-role para escribir `tier='premium'` SIN verificar que `details.customId === profileId` ni que la cookie de sesión es del mismo usuario. Upgrade-jacking explotable observando una `subscriptionId ACTIVE` legítima | Tras `getSubscriptionDetails`, comparar `details.customId === profileId`. Adicionalmente leer cookie y validar `user.id === profileId`. Sin doble validación, el endpoint es compromisible. **S** |
| SEG-06 | 🟠 ALTA | Seguridad | `src/app/api/battlelog/route.ts:5-14`, `src/app/api/club/route.ts:4-13` | Rutas POST públicas sin auth ni rate-limit que reenvían a Supercell. 1k tags/seg × 1h → ban del rango IP del Cloudflare Worker proxy → sitio entero offline para `/profile`, `/compare`, `/subscribe`, calendario, club chart | Rate-limit por `x-forwarded-for` con Upstash Redis (30 req/min/IP). Si solo se llama desde server (`/profile/[tag]/page.tsx`), eliminar endpoint y usar `fetchBattlelog` directo. **M** |
| SEG-07 | 🟠 ALTA | Seguridad | `next.config.ts:13-26` | CERO `Content-Security-Policy`. App carga scripts de cdn.brawlify, brawlapi, AdSense, GA, y embebe JSON-LD via `dangerouslySetInnerHTML`. Cualquier XSS futuro = exfil libre. HSTS sin `preload` | CSP con allow-list explícita: `script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://pagead2.googlesyndication.com; img-src 'self' data: https://cdn.brawlify.com https://api.brawlapi.com; …`. Deploy iterativo en `Report-Only` antes de enforcing. **M** |
| TEST-01 | 🟠 ALTA | Testing | `.github/workflows/` (no existe) | [recalibrado de CRÍTICA — no produce downtime/RCE por sí sola] No hay workflow GitHub Actions, ni husky, ni Vercel CI que ejecute `npm test`. La inversión en 758 tests depende de disciplina manual. La promesa "auth contract test por route" no se hace cumplir | `.github/workflows/ci.yml` con jobs `typecheck` (`npx tsc --noEmit`), `unit` (`npx vitest run`), `e2e` (`npx playwright test`). Required checks en `main`. **S** |
| TEST-02 | 🟠 ALTA | Testing | `src/app/api/{brawlers,maps,draft/data,draft/maps,notify/signup,profile/check-premium,meta,meta/brawler-detail}/route.ts` | 8 de 27 routes sin test integración — incluye `/api/notify/signup` (Telegram, side-effect 3rd party), `/api/profile/check-premium` (rama monetización), `/api/meta` raíz (consumida por `/picks` SSR) | Crear los 8 `-auth-contract.test.ts` con patrón `fromMock + queueByTable`. **M** (8 routes × ~30 LoC × 4 casos) |
| TEST-03 | 🟠 ALTA | Testing | `src/__tests__/integration/api/battles-auth-contract.test.ts:26-34`, `meta-poll-adaptive.test.ts:44-57`, `brawler-detail.test.ts:7-16`, `brawler-metadata.test.ts:158-168` | Builders `for (const m of methods) builder[m] = () => builder` aceptan cualquier método sin validar columnas/shapes. Renombrar `total → total_battles` no rompe ningún test; el route consumiría `undefined` y los tests siguen verdes | Tipar el builder con `Database['public']['Tables']['meta_stats']['Row']` (depende de ARQ-01). Contract test nightly contra preview branch de Supabase. **M** |
| TEST-04 | 🟠 ALTA | Testing | toda `src/__tests__/**` | Cero tests para IDOR, XSS, path-traversal, Bearer-token-rechazado-en-cookie-routes, webhook signature replay. Bypass del contrato `cookie-only` no testeado | `src/__tests__/security/`: IDOR sobre `/api/battles?aggregate=true`, XSS rendering en `ProfileHeader`, path-traversal sobre `[brawlerId]`, Bearer enviado a cookie-route → 401. **M** |
| ARQ-04 | 🟡 MEDIA | Arquitectura + Nombrado | `src/lib/telegram.ts` ⊕ `src/lib/telegram/{sender,dispatcher,...}.ts` | Coexisten archivo `telegram.ts` (con `notify()`) y directorio `telegram/` (con `sendTelegramMessage()`). Dos funciones semánticamente equivalentes; cualquier cambio en headers/timeouts requiere editar dos sitios | Mover `notify()` a `src/lib/telegram/notify.ts` como wrapper de `sendTelegramMessage(env.TELEGRAM_CHAT_ID, msg)`. Borrar `lib/telegram.ts`. Actualizar 3 imports. **S** (30 min) |
| ARQ-07 | 🟡 MEDIA | Código Limpio | `src/lib/analytics/compute.ts:1-963` | 963 LoC orquestando 21 sub-aggregaciones. `AdvancedAnalytics` interface con 35 campos. SRP empezando a tensar | Dividir en `analytics/compute/{overview,brawler,mode,map,matchups,trios,tilt,sessions,powerLevel,gadget,weeklyPattern}.ts` con barrel index. **M** (1 día) |
| ARQ-09 | 🟡 MEDIA | Nombrado + Datos | `src/lib/supabase/types.ts:14-28`, propagado a 8+ ficheros | Naming `ls_*` (legado Lemon Squeezy) sobre stack PayPal. `webhooks/paypal/route.ts:84` literalmente: `ls_customer_id = 'paypal_${subscriptionId}'`. Vector cognitivo invertido para nuevos contribuidores | Migration `022_rename_ls_to_subscription.sql`. Update `Profile` types y 8 callsites con `replace_all`. Actualizar `MEMORY.md` `premium_architecture.md`. **M** (3-4 h) |
| ARQ-10 | 🟡 MEDIA | Código Limpio + Resiliencia | `src/components/landing/InputForm.tsx:46`, `:75`, `src/hooks/useBrawlerTrends.ts:62` | 3 errores ESLint `react-hooks/set-state-in-effect`, sin gate de CI ni husky. `usePlayerData.ts:57` ya tiene `eslint-disable` con justificación; los 3 hermanos no | `eslint-disable-next-line` con razón en los 3 sites + `"lint:strict": "eslint . --max-warnings 0"` en package.json + husky pre-commit. **S** (1 h) |
| LOG-10 | 🟡 MEDIA | Rendimiento | `src/hooks/useProAnalysis.ts:14` | `const cache = new Map<string, ProAnalysisResponse>()` a nivel módulo, sin TTL ni eviction. Usuario premium navegando 50 maps × 4 modes × 4 windows × 100KB ≈ 80 MB residente | TTL 30 min (matching `s-maxage` server) + LRU `maxSize=50`. Considerar migrar todos los hooks de fetch a SWR/react-query. **S** puntual / **M** si se migra |
| LOG-11 | 🟡 MEDIA | Resiliencia | `src/hooks/useClubTrophyChanges.ts:103-150` | `useEffect(() => load(members))` sin `AbortController`. Si `members` cambia mid-loop, el `Promise.allSettled` sigue ejecutando y mezcla filas del club anterior con el actual durante segundos | `controllerRef = useRef<AbortController>()`: cancelar al inicio de nuevo `load()`, comprobar `signal.aborted` antes de cada `setResults`. Pasar signal a `fetch`. **S** |
| LOG-12 | 🟡 MEDIA | Datos | `src/lib/battle-parser.ts:9-18` | `parseBattleTime` parsea por slice posicional sin regex. Entrada malformada produce ISO sintácticamente válido pero semánticamente basura → escribe a `battles.battle_time` sin notar | Reusar `parseSupercellTime` (que ya valida con regex y devuelve `null`) en `parseBattle:134`, o validar con regex antes del slice y throw. **S** |
| LOG-13 | 🟡 MEDIA | Datos | `src/hooks/useBattlelog.ts:55-66`, mismo patrón en `useClubTrophyChanges.ts:42-50` y otros 3 hooks | Cache localStorage sin `_schemaVersion`. Cambio de interfaz no rompe la caché vieja hasta TTL — componente que destructura nuevo campo crashea | Añadir `_schemaVersion: number` al payload y comparar con constante de módulo. `if (cached._schemaVersion !== CURRENT) return null`. Patch global 5 hooks. **S** |
| LOG-14 | 🟡 MEDIA | Datos | `src/lib/paypal.ts:214-218` | `BILLING.SUBSCRIPTION.UPDATED + status:CANCELLED` devuelve `tier:'free'`; `BILLING.SUBSCRIPTION.CANCELLED` devuelve `tier:'premium'` (grace). Si UPDATED llega después por race, grace se pierde | Devolver `tier:'premium'` también en UPDATED+CANCELLED. Persistir `last_event_at` y rechazar updates con `event.create_time < profile.last_event_at`. **S** |
| PERF-03 | 🟡 MEDIA | Rendimiento | `src/app/api/analytics/route.ts:46-57`, `src/lib/analytics/compute.ts:26-963` | `computeAdvancedAnalytics` ejecuta 27 funciones puras secuenciales sobre hasta 5000 battles en el handler. ~150-400 ms CPU bloqueante por request. Bloquea event loop, escalado horizontal no ayuda | (a) Materializar en `battle_analytics_daily` con cron diario y servir de DB (patrón `brawler_trends`); o (b) reescribir compute.ts en una pasada. **M-L** |
| PERF-04 | 🟡 MEDIA | Rendimiento + Resiliencia | `src/app/api/draft/maps/route.ts:9-41` | Cache module-level sin negative caching. Cold start con N instancias = stampede a BrawlAPI. Si BrawlAPI cae, cada instancia devuelve `new Map()` silenciosamente | Eliminar cache de módulo, confiar en `next: { revalidate: 86400 }` (ya hace dedup). Fallback persistente en Supabase `map_images_cache` con escritura semanal (patrón `brawler_trends`). **M** |
| PERF-05 | 🟡 MEDIA | Rendimiento | `src/app/api/meta/pro-analysis/route.ts:92-128, 229-251, 386-393, 462-509` | 4 queries principales seriadas (no dependientes entre sí), ~150-300 ms cada una. 50 maps × 8 modes × 4 windows = 1600 cache entries frías; enumeración paga DB | `Promise.all([statsRows, stats7d, stats30d, matchupRows])`. Whitelist de mapas vivos (cache 1h) → 400 sin tocar DB si `map` no está. **S** |
| PERF-06 | 🟡 MEDIA | Rendimiento | `src/app/api/cron/meta-poll/route.ts:394-437` | For-of secuencial 1000 jugadores × `META_POLL_DELAY_MS`. Un battlelog a 8s estira cron > 300s y dispara `timeBudgetExit` | `p-limit` concurrencia 4 + combinarlo con PERF-01 (timeout 8s). Sin timeout, paralelizar amplifica el riesgo. **M** |
| PERF-07 | 🟡 MEDIA | Rendimiento | `src/app/api/draft/data/route.ts:36-93` | Endpoint público (`/picks`, draft simulator) sin `Cache-Control` ni `s-maxage`, vs `/api/meta` que sí tiene 1800s. 4 queries Supabase secuenciales por request | Añadir `s-maxage=900`. Paralelizar 3 queries iniciales con `Promise.all`. **S** |
| RES-02 | 🟡 MEDIA | Resiliencia | `src/app/api/checkout/paypal/confirm/route.ts:35-57`, `src/app/api/webhooks/paypal/route.ts:78-92` | Confirm y webhook se ejecutan concurrentes por diseño. Webhook envía CANCELLED durante cancel inmediato, llega callback con ACTIVE → sobreescribe cancelled con active. `alreadyApplied` no protege | (a) Eliminar escritura desde `/confirm` (decisión documentada — webhook autoritativo); o (b) `UPDATE … WHERE id=… AND ls_subscription_status NOT IN ('cancelled','expired')`. **S** |
| RES-04 | 🟡 MEDIA | Observabilidad | General — todas las routes en `src/app/api/**/route.ts` | `console.error('[meta-poll] Fatal error:', err)` etc. sin estructurar. Imposible correlacionar el flujo de un usuario que reporta "no se actualizó mi premium" sin `request_id` y `user_id` | `console.log(JSON.stringify({ level, msg, request_id, user_id, ...ctx }))` o pino lite. Generar `request_id` en `proxy.ts`, propagar via header `x-request-id`. **M** |
| SEG-05 | 🟡 MEDIA | Seguridad | `src/lib/constants.ts:3` (`/^#[0-9A-Z]{3,20}$/i`) vs `src/app/api/profile/route.ts:4` y `src/app/api/club/route.ts:8` (`/^#[0-9A-Z]{3,12}$/`) | `/api/calculate`, `/api/battlelog`, `/api/player/tag-summary` aceptan 3-20; `/api/profile` y `/api/club` rechazan ≥13. Tag legítima de 15 chars puede crear perfil bloqueado tras signup exitoso | Reemplazar copias inline por `import { PLAYER_TAG_REGEX } from '@/lib/constants'`. Helper compartida para upper-case. **S** |
| SEG-08 | 🟡 MEDIA | Seguridad | `src/app/api/battles/route.ts:58-60`, `src/app/api/profile/route.ts:53-55` | `error.message` de Supabase JS al cliente expone constraint names. Para 23505 unique en `profiles` revela `profiles_player_tag_key` — convierte POST en oráculo de existencia de tags | Mensajes genéricos al cliente, `console.error(error)` server-side. Mapear códigos PostgREST a respuestas estables (`23505 → "Tag already linked"`). **S** |
| SEG-09 | 🟡 MEDIA | Seguridad | `src/app/api/notify/signup/route.ts:10-32` | Sin idempotencia. Usuario autenticado puede reinvocar y disparar Telegram repetidamente. Mensaje incluye `email` (GDPR retention en Telegram) | Flag persistente `profiles.signup_notified_at`. O mover a Supabase auth hook que crea `auth.users`. Hashear/truncar email. **S** |
| TEST-05 | 🟡 MEDIA | Testing | `e2e/landing.spec.ts:36,40,84`, `e2e/locale.spec.ts:84`, `e2e/meta-pro.spec.ts:64,89`, `e2e/picks.spec.ts:57` | 7 `waitForTimeout` mágicos (200/500/1000/2000/5000 ms). En CI con runner cargado → flakes que `retries:2` esconden. `landing.spec.ts:84` aceptar localStorage como assertion en lugar de redirect URL | Sustituir cada `waitForTimeout` por `page.waitForFunction(...)` ligado a condición real. Para redirect: `await expect(page).toHaveURL(...)`. **S** |
| TEST-06 | 🟡 MEDIA | Testing | `e2e/picks.spec.ts:60`, `e2e/meta-pro.spec.ts:67`, `e2e/landing.spec.ts:43-44`, `e2e/share.spec.ts:54` | Smokes "zero console.error" pasan si la página renderiza esqueleto vacío. CLAUDE.md ya lo admite. `share.spec.ts:43-55` ni siquiera tiene `expect` final | Migrar cada smoke a ≥ 1 aserción positiva del producto visible. Para `share`, asegurar que `ShareCard` está visible. **S** |
| TEST-07 | 🟡 MEDIA | Testing | `src/__tests__/unit/lib/analytics-compute.test.ts:137,143,171,183,199,217,398,424,488,493,537,543,558,813,818,835` (16), `calculate.test.ts:109-116` (7) | 35+ `toBeDefined()` y 9 `toBeTruthy()`. `expect(result.tilt.wrNormal).toBeDefined()` no diferencia 42, 0, null, NaN | Sustituir por comparación numérica (`toBeGreaterThan(0)`/`toBeCloseTo`). En `calculate.test.ts` reemplazar bloque por `toMatchObject` con valores esperados. **S** |
| TEST-08 | 🟡 MEDIA | Testing | `vitest.config.ts:1-19` | `@vitest/coverage-v8` instalado pero `coverage:` block ausente. Sin thresholds → un PR que añade 500 LoC sin tests no falla | `coverage: { provider:'v8', reporter:['text','json-summary','lcov'], exclude:[...], thresholds: { lines:70, functions:70, branches:60, statements:70 }}`. Activar en CI. **S** |
| TEST-09 | 🟡 MEDIA | Testing | repo-wide; `package.json` sin `@stryker-mutator/*` | Sin mutation testing. Métricas que dirigen UI ("intervalo de confianza" Wilson) podrían tener un signo invertido y los 6 tests pasarían | Stryker opcional, no-blocking, solo módulos críticos: `analytics/stats.ts`, `analytics/compute.ts`, `draft/scoring.ts`, `brawler-metadata.ts`. Ejecución manual/nightly. **M** |
| TEST-10 | 🟡 MEDIA | Testing | `src/__tests__/integration/api/meta-poll-adaptive.test.ts:143-145` (top-level), `cron-sync.test.ts:23-25` (beforeEach), `unit/lib/anonymous-visits.test.ts:34-35`, `telegram/queries.test.ts:47-48` | `process.env` mutado sin restaurar entre tests/ficheros. Test posterior que asume "missing secret" queda contaminado. Patrón correcto en `paypal-webhook.test.ts:72,89-95` y `telegram/sender.test.ts:14-18` (capture+restore) | `vi.stubEnv()` + `vi.unstubAllEnvs()` en `afterEach`. O helper `withEnv({...}, () => {})`. **S** |
| TEST-11 | 🟡 MEDIA | Testing | `e2e/compare.spec.ts:70-86`, `e2e/brawler-page.spec.ts:14`, `e2e/navigation.spec.ts:15` | E2E navegan a `/profile/#TAG/...` pegados a Supercell real. Si VPS proxy cae, mantenimiento Supercell, 503, rate-limit → tests fallan. `retries:2` permite que un bug intermitente se mergee | Modo "E2E offline" con `page.route('**/api/battlelog/**', route => route.fulfill({ json: FIXTURE }))`. Fixtures versionados en `e2e/fixtures/`. Mantener "live" como suite separada opcional. **M** |
| ARQ-12 | 🟢 BAJA | Arquitectura | `src/app/api/auth/callback/route.ts:2`, `src/app/api/cron/meta-poll/route.ts:2`, `src/app/api/cron/sync/route.ts:2`, `src/app/api/meta/pro-analysis/route.ts:2` | 4 routes importan `createServerClient` directo en vez del wrapper canónico `lib/supabase/server`. Cada uno reimplementa config inline; cualquier cambio sameSite/secure/domain → 5 edits | Helper `createServiceClientNoCookies()` en `lib/supabase/server.ts` para crones. `auth/callback` puede usar `createClient()` existente. Reescribir 4 imports. **S** (1 h) |
| ARQ-13 | 🟢 BAJA | Código Limpio | `src/components/analytics/MasteryChart.tsx:11-28` ⊕ `src/lib/analytics/types.ts:141-154` | `MasteryPoint`/`BrawlerMastery` declarados localmente y en types canónico. Estructuralmente compatibles HOY; nuevo campo en canónico → componente sigue compilando contra su tipo local sin obtener el campo | Borrar interfaces locales en `MasteryChart.tsx`. `import type { BrawlerMastery, MasteryPoint } from '@/lib/analytics/types'`. **S** (15 min) |
| ARQ-15 | 🟢 BAJA | Documentación | `src/lib/draft/constants.ts:158`, `src/app/api/cron/meta-poll/route.ts:48`, `CLAUDE.md` | CLAUDE.md describe "Base batch 200 / Hard cap 600 / top-up 100" que no existe; código actual es probabilistic-sampling MAX_DEPTH=1000 | Actualizar CLAUDE.md "Data Pipeline → cron meta-poll" con sampler probabilístico real Sprint F. **S** (15 min) |
| LOG-15 | 🟢 BAJA | Datos | `src/lib/brawler-detail/trend.ts:44-53` | `r.date >= d7ago` lex comparison. Si llega `r.date = "2026"` (truncada por bug futuro), cae en bucket "previous 7d" sin warning | Validación regex `/^\d{4}-\d{2}-\d{2}$/` al inicio del loop, o `new Date(r.date)` y `isFinite(d.getTime())`. **S** |
| LOG-16 | 🟢 BAJA | Datos | `src/lib/analytics/compute.ts:695-706` | Umbral fijo ±50 trofeos para clasificar "weak/even/strong". En low-trophy 50 trofeos = enorme; en high-trophy = ruido. Sesgo sistemático | Escalar proporcionalmente (5% de `myTrophies`), o cambiar a percentiles del bucket. Decisión de producto, no técnica. **S** |
| LOG-17 | 🟢 BAJA | Rendimiento | `src/app/api/cron/meta-poll/route.ts:393-417` | `computeMinLive` itera live keys cada player. 600 × 30 = 18k iters extra. No crítico (HTTP domina) pero comentario "minLive reflects any counts that grew" es impreciso | Documentar granularidad real (por-jugador, no por-batalla), o tracking incremental cuando `battlesByMapMode[key]++`. **S** |
| LOG-18 | 🟢 BAJA | Código Limpio | `src/lib/analytics/compute.ts:736-738` | `comfort = wilson*0.6 + (group.length/maxGames)*30 + consistency*10` mezcla escalas (porcentaje vs ratio×30 vs ratio×10) sin documentar por qué | Comentario explicativo o `COMFORT_WEIGHTS = { wr:0.6, popularity:0.3, consistency:0.1 }` y multiplicar todos por 100. **S** |
| LOG-19 | 🟢 BAJA | Datos | `src/lib/draft/meta-accumulator.ts:26-44` | `processBattleForMeta` permite `myBrawlerId === 0` (sentinel) — escribe a `meta_stats`. Aguas abajo "0 - undefined" o silenciado | Guard `if (!Number.isFinite(myBrawlerId) \|\| myBrawlerId <= 0) return`. Mismo patrón en `compute.ts` para todos los `?? 0`. **S** |
| RES-03 | 🟢 BAJA | Resiliencia | `src/app/api/webhooks/paypal/route.ts:97-98`, `src/app/api/notify/signup/route.ts:24-26` | `await notify(...)` síncrono pre-response. Telegram cuelga 30s → webhook tarda 30s → PayPal reintenta. Patrón correcto `after()` ya usado en `/api/calculate` | Envolver en `after(async () => notify(...))` los `await notify` no-críticos. **S** |
| RES-05 | 🟢 BAJA | Seguridad + Resiliencia | `src/lib/paypal.ts:175-202` | `paypal-cert-url` se reenvía sin validar dominio. Hoy no hay vector porque el GET lo hace PayPal; cuando se migre a verificación local (RES-01) sería SSRF | Cuando se haga RES-01, hardcodear allowlist `https://api.paypal.com/v1/notifications/certs/...` o `sandbox`. **S** (prerequisito de RES-01) |
| SEG-10 | 🟢 BAJA | Seguridad | `src/app/api/profile/check-premium/route.ts:10-37` | Endpoint público responde true/false por tag arbitrario sin rate-limit. Permite enumerar quién es premium en la base | Rate-limit IP. Considerar mover a `/api/profile/me` (auth required). Side-channel timing: retraso constante. **S** |
| TEST-12 | 🟢 BAJA | Testing | `src/__tests__/unit/components/analytics/TopBrawlersGrid.test.tsx:142-146`, `analytics-compute.test.ts:401-407`, `e2e/share.spec.ts:43-55` | "No crashea" como única aserción — componente puede renderizar basura sin throw. Test redundante si el siguiente bloque ya hace render assertion | Eliminar redundantes, o reforzar con aserción positiva (`getAllByText(/CROW\|BULL\|PIPER/).length === 3`). **S** |
| TEST-13 | 🟢 BAJA | Testing | `src/__tests__/unit/components/{ConfidenceBadge,MapCard,MetaIntelligence,BrawlImg,InputForm,UrlFlashMessage}.test.tsx` (+ varios) | Mocks inline `useTranslations: () => (key) => map[key] ?? key` silencian `FORMATTING_ERROR` cuando copy nuevo introduce `{param}`. Strict `mockNextIntl` solo en 3 ficheros pilot | Migrar componentes restantes al strict mock helper. Sustitución mecánica ~5 LoC por fichero. **S** |
| TEST-14 | 🟢 BAJA | Testing | `src/__tests__/integration/api/player-tag-summary.test.ts` | No cubre tag con espacios `'  #ABC123  '`, unicode, longitud máxima (URL injection?), ni rate-limit response 403/429 de Supercell | Añadir 4 casos al test. **S** |
| TEST-15 | 🟢 BAJA | Testing | `src/test/setup.ts` (1 línea) | Setup global mínimo: no resetea `localStorage` entre tests, no reset mocks. Cada test llama `vi.clearAllMocks()` manualmente — fácil olvidar | `afterEach(() => { vi.clearAllMocks(); vi.unstubAllEnvs(); localStorage.clear() })` global. **S** |

---

## 🚨 Plan de Choque — siguientes 24 h

Los 3 hallazgos de mayor riesgo. Resolverlos antes que cualquier otro trabajo. Una de las 4 CRÍTICAS (MIX-03 — `pickRate` truncado en brawler-detail) queda fuera del top-3 por menor explotabilidad activa que SEG-01 y menor superficie de impacto inmediato que LOG-01/LOG-02; debe ejecutarse esta misma semana.

### 1. SEG-01 — Open redirect en `/api/auth/callback` vía `?next=`

- **Por qué ahora:** Vector OAuth-phishing **explotable hoy** con un único URL malicioso (`?next=//evil.com/phish`); cualquier usuario click → sesión legítima creada en cookie de `brawlvision.com` y aterrizaje en página atacante. Fix de 15 LoC.
- **Coste estimado:** S (≤ 1 h)
- **Owner sugerido:** Backend Lead / Security
- **Detalle:** ver entrada `SEG-01` en la tabla.

### 2. LOG-02 — Webhook PayPal: idempotencia ANTES del side-effect → updates perdidos

- **Por qué ahora:** Pérdida directa de ingresos: usuario paga, `profiles.update` falla transitoriamente, retry de PayPal ve fila idempotente y devuelve `skipped:true`. El usuario queda `tier:'free'` permanentemente, requiriendo soporte manual para reconciliar. Activo desde el sprint actual.
- **Coste estimado:** S (≤ 2 h con test de integración)
- **Owner sugerido:** Backend Lead
- **Detalle:** ver entrada `LOG-02` en la tabla.

### 3. LOG-01 — `compute_brawler_trends()` SQL no filtra `source` (mezcla PRO + users)

- **Por qué ahora:** Corrupción silenciosa **ya en producción**: el badge "trend PRO" del home se sirve a 100% de visitantes con datos contaminados por rachas personales de usuarios premium. Bloquea LOG-04 (mismo PR — corrige también la rama TS inline). Sin fix, cualquier nueva analítica derivada de `brawler_trends` heredará la contaminación.
- **Coste estimado:** S (1-2 h: migración + fix TS + test SQL/integración)
- **Owner sugerido:** Data / Backend Lead
- **Detalle:** ver entrada `LOG-01` en la tabla.

---

## Notas

- **IDs:** `ARQ-NN` (arquitectura), `LOG-NN` (lógica/datos), `SEG-NN` (seguridad), `PERF-NN` (rendimiento), `RES-NN` (resiliencia), `TEST-NN` (testing), `MIX-NN` (multi-categoría tras dedupe).
- **MIX entries:**
  - **MIX-01** = ARQ-11 ⊕ PERF-02 (rate-limit in-memory en `/api/analytics`).
  - **MIX-02** = ARQ-06 ⊕ LOG-08 (manual sync no escribe meta_stats).
  - **MIX-03** = LOG-03 ⊕ PERF-08 (brawler-detail trunca pickRate).
- **Recalibración aplicada:** `TEST-01` bajó de CRÍTICA → ALTA. La ausencia de CI no produce downtime ni corruption por sí sola; multiplica el coste de cualquier regresión futura. Sigue siendo prioridad alta porque hace cumplir todos los demás contratos de testing.
- **Ámbitos no auditados (declarados por las fases):**
  - Estado real de `pg_cron` jobs (no validable sin acceso DB).
  - Comportamiento dinámico de `/api/auth/callback` con payload malicioso (no se ejecutó staging).
  - Métrica real de coverage (no había umbral de referencia previo, pendiente de TEST-08).
  - Migrations 001-019 (solo se profundizó 020/021 por relevancia a `compute_brawler_trends`).
  - RLS Supabase a nivel SQL (auditado solo por inspección de uso `createClient()` vs `createServiceClient()` en código).
- **Cada CRÍTICA y la mayoría de ALTAS tienen evidencia textual citable** (ruta + línea + snippet) en el reporte de fase original.
- **Las 4 fases ejecutaron exitosamente.** Todos los reportes pasaron validación de estructura (secciones 1-5 del template, prefijos correctos, tabla de hallazgos completa).

---

## Próximos pasos sugeridos

1. **24 h:** ejecutar Plan de Choque (SEG-01, LOG-02, LOG-01) — todos S, total ≤ 5 h con tests.
2. **Esta semana:**
   - Resto de CRÍTICAS: **MIX-03** (brawler-detail trunca pickRate — RPC SQL `sum_meta_stats_total`).
   - ALTAS con coste S: SEG-02 (bump `next@16.2.4`), SEG-03 (validar locale auth/callback), SEG-04 (validar `customId`/cookie en paypal/confirm), LOG-04 (filtro source en TS — mismo PR que LOG-01), LOG-05, LOG-06, LOG-09, ARQ-03, ARQ-10.
3. **Sprint actual (2 sem):** resto de ALTAS:
   - **Datos/Tipado:** ARQ-01 (gen types Supabase — desbloquea TEST-03), ARQ-08 (test SQL↔TS parity), MIX-02 (helper sync compartido).
   - **Seguridad:** SEG-06 (rate-limit `/api/battlelog`+`/api/club`), SEG-07 (CSP en Report-Only), MIX-01 (Upstash `/api/analytics`).
   - **Pagos/Resiliencia:** RES-01 (verify webhook local).
   - **Performance:** PERF-01 (timeouts en `lib/http.ts`).
   - **Producto:** ARQ-02 (extraer pro-analysis), ARQ-05 (SafeAdSlot en 8 páginas), ARQ-14 (`/api/player/club-summary`).
   - **Testing:** TEST-01 (CI workflow), TEST-02 (8 routes faltantes), TEST-04 (security tests).
4. **Backlog técnico:** todas las MEDIA agrupadas por categoría/owner para tickets dedicados. Las 7 MEDIA de testing (TEST-05..11) son un mini-sprint cohesivo.
5. **BAJAS:** consolidar en una PR de sweep cuando un dev tenga un rato muerto.
6. **Próxima auditoría Panóptico:** sugerida en **6-8 semanas** (sprint+1 después de cerrar todas las ALTAS) para verificar regresiones y medir progreso.
